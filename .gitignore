# Editor / OS detritus
.DS_Store
*.swp
*.swo
*~

# Per-clone tooling state (do NOT commit)
.claude/
CLAUDE.md

# Build leftovers
new.bashrc.tmp
*.tmp

# Generated demo: `demo/demo.gif` IS committed (the canonical render shipped
# in the repo; regenerate with `make demo`). Other scratch outputs are not.
demo/*.mp4
demo/*.webm
demo/*.png

# Coverage / temp test output
tests/.tmp/
